
# Optional dependencies for the QualiJournal admin service. These are not
# strictly required to run the application but may be needed for optional
# features such as database connectivity or PDF/CSV export. Keep this list
# separate so that the core dependencies remain lightweight.
# (Currently empty; add optional packages here as needed.)
