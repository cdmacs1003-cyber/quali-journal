# Rollback Note

No rollback is needed for R9ZCO materialization unless the new untracked ProofPack is rejected.

Do not reset/restore/clean/stash.

If source/test/schema rollback is later approved, use a separately approved targeted revert or patch against commit d6ad3d7.

If proofpack materialization is rejected, handle only through a separately approved cleanup/remediation packet.
