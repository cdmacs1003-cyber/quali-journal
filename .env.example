# App
APP_ENV=dev
PORT=8080

# DB 모드 (표준 값: local | cloud | test)
QUALI_DB_MODE=local

# Cloud SQL(PostgreSQL) - cloud 모드일 때만 사용
INSTANCE_CONNECTION_NAME=<project>:<region>:<instance>
DB_USER=<user>
DB_PASS=<secret>
DB_NAME=<db>
