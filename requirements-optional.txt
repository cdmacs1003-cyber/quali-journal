# QualiNews — optional dependencies

# HTML/XML parsing & RSS
html5lib>=1.1
feedparser>=6.0

# Export/convert helpers (선택)
# Linux/macOS에서 HTML→PDF 변환
weasyprint>=61.0 ; platform_system != 'Windows'
# Windows는 wkhtmltopdf(별도 설치) 권장

# Search / Index (장기 로드맵)
elasticsearch>=8.15
